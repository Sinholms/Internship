// Shim - keep backward compat, but prefer direct imports from lib/
// Mirip reference: logic lives in src/lib/getStrapiImageUrl.ts
export { getStrapiImageUrl, default } from '../getStrapiImageUrl';
export { formatDateID, formatDate } from '../formatDate';
